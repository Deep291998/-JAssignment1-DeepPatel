<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
    />
    <title>Recipe Management System</title>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarTogglerDemo01"
          aria-controls="navbarTogglerDemo01"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
          <a class="navbar-brand" href="/">MarketMan</a>
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link" href="/recipes">Recipes</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/add-recipe">Add Recipe</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <main class="container p-4">
      <h1 class="page-title">Delete Recipe</h1>
      <form action="/delete-recipe-submit" method="post">
        <input type="hidden" name="recipeId" value="<%= recipeData._id %>">
          <div class="my-3">
            <p class="lead">Are you sure you want to delete <span class="fw-bold text-dark"><%= recipeData.name %></span></p>
            <p class="lead">Please keep in mind that this action is not revertable</p>
          </div>
          <div class="d-flex align-items-center gap-4 mb-3 pt-3">
            <a href="/recipes" class="btn btn-light">Cancel</a>
            <button type="submit" class="btn btn-danger">Delete Recipe</button>
          </div>
      </form>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
